1. Write an OpenAPI 3.0 YAML for CRUD operations for products , the product would at least have these fields id (auto), name, description, price, quantity, category.


2.have multiple files and import them to single openApi.yaml specialy components and Error Responses.

3.Add an OpenAPI 3.0 YAML path definition for a GET /events/stream endpoint that uses Server-Sent Events (SSE) for real-time notifications